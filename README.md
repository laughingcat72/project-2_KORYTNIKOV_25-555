📊 Примитивная база данных на Python
Простая реляционная база данных, реализованная на Python с SQL-подобным синтаксисом команд.

🚀 Возможности
✅ Создание и удаление таблиц

✅ CRUD-операции (Create, Read, Update, Delete)

✅ Поддержка типов данных: int, str, bool

✅ Автоматический ID с автоинкрементом

✅ Фильтрация данных по условиям WHERE

✅ Подтверждение опасных операций

✅ Кэширование запросов для ускорения

✅ Логирование времени выполнения

✅ Сохранение данных в JSON-файлы

✅ Красивый вывод таблиц через PrettyTable

📁 Структура проекта
text
database/
├── __init__.py
├── main.py          # Точка входа
├── core.py          # Основная логика БД
├── engine.py        # Командный интерфейс
├── utils.py         # Работа с файлами
├── decorators.py    # Декораторы
└── constants.py     # Константы
🛠 Установка
Способ 1: Используя Poetry (рекомендуется)
bash


# Установите зависимости
make install
# или
poetry install
Способ 2: Используя pip
bash
# Установите зависимости
pip install prettytable

# Запустите проект
python -m database.main
🎮 Использование
Запуск программы:
bash
make project
# или
poetry run project
# или
python -m database.main
Доступные команды:
Управление таблицами:
sql
create_table users name:str age:int active:bool
drop_table users
list_tables
Работа с данными:
sql
insert into users values ("Иван", 25, true)
select from users
select from users where age = 25
update users set active = false where name = "Иван"
delete from users where ID = 1
info users
Общие команды:
sql
help      # Справка по командам
exit      # Выход из программы

Количество записей: 1
🔧 Make команды
bash
make install        # Установить зависимости
make project        # Запустить проект
make lint           # Проверить код линтером
make format         # Форматировать код
make build          # Собрать пакет
make clean          # Очистить временные файлы
📊 Поддерживаемые типы данных
Тип	Пример	Описание
int	42, -15	Целые числа
str	"Привет", 'Мир'	Строки (в кавычках)
bool	true, false, 1, 0	Логические значения
🛡 Особенности реализации
Декораторы:
@handle_db_errors - обработка ошибок базы данных

@confirm_action - подтверждение опасных операций

@log_time - замер времени выполнения


.

💡 Советы по использованию
Используйте кавычки для строк: "Имя Фамилия"

ID генерируется автоматически: не указывайте его при вставке

Регистр не важен для true/false: TRUE, True, true работают одинаково

Для выхода используйте exit или Ctrl+C

Все изменения сохраняются автоматически после каждой опера

Ссылка на видео https://drive.google.com/file/d/1wVz96MxYIYQhGuAlCHYQlDlXVueXG97d/view?usp=sharing